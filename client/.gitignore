// This file is a .gitignore template that specifies which files and directories Git should ignore.

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules // Ignores the node_modules directory where npm packages are installed
/.pnp // Ignores the .pnp file used by Yarn Plug'n'Play feature
.pnp.js // Ignores the .pnp.js file used by Yarn Plug'n'Play feature

# testing
/coverage // Ignores the coverage directory used for code coverage reports

# production
/build // Ignores the build directory where the production-ready files are generated

# misc
.DS_Store // Ignores .DS_Store files created by macOS for storing folder attributes
.env.local // Ignores the local environment variables file
.env.development.local // Ignores the local development environment variables file
.env.test.local // Ignores the local test environment variables file
.env.production.local // Ignores the local production environment variables file

npm-debug.log* // Ignores npm debug log files
yarn-debug.log* // Ignores Yarn debug log files
yarn-error.log* // Ignores Yarn error log files